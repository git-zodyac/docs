import { CodeSnippetPipe } from './code-snippet.pipe';

describe('CodeSnippetPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeSnippetPipe();
    expect(pipe).toBeTruthy();
  });
});
