import { RouteContainerDirective } from './route-container.directive';

describe('RouteContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteContainerDirective();
    expect(directive).toBeTruthy();
  });
});
